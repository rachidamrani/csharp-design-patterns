/*
 * Define an interface for creating an object, but let the subclasses decide
 * which class to instantiate. The Factory method lets a class defer instantiation
 * it uses to subclasses
 */
 
 Console.WriteLine();